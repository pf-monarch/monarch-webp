import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Server, Plane, TrendingUp, Building2 } from "lucide-react";
import { motion } from "framer-motion";

const features = [
  {
    icon: Server,
    title: "Organized Server",
    description: "Well-structured and professionally managed virtual airline environment with clear procedures and systems.",
    color: "text-green-600"
  },
  {
    icon: Plane,
    title: "Diverse Fleet",
    description: "Extensive range of aircraft types and models to suit every route and pilot preference in our virtual fleet.",
    color: "text-blue-600"
  },
  {
    icon: TrendingUp,
    title: "High PF Publicity",
    description: "Strong presence and excellent reputation within the PilotEdge community with high visibility and recognition.",
    color: "text-purple-600"
  },
  {
    icon: Building2,
    title: "Only Chartering Company in PF",
    description: "Unique position as the exclusive chartering service provider within the PilotEdge network.",
    color: "text-amber-600"
  }
];

export default function ServiceFeatures() {
  return (
    <div>
      {/* Section Header */}
      <motion.div 
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.6 }}
        viewport={{ once: true }}
        className="text-center mb-12"
      >
        <h2 className="text-3xl md:text-4xl font-bold text-slate-800 mb-4">
          Why Choose Monarch Air Virtual
        </h2>
        <p className="text-lg text-slate-600 max-w-2xl mx-auto">
          Experience the difference with our premium virtual airline services and commitment to excellence
        </p>
      </motion.div>

      {/* Features Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        {features.map((feature, index) => (
          <motion.div
            key={index}
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: index * 0.1 }}
            viewport={{ once: true }}
          >
            <Card className="h-full hover:shadow-lg transition-all duration-300 group border-0 bg-white">
              <CardContent className="p-8 text-center">
                <div className="relative mb-6">
                  <div className={`w-16 h-16 mx-auto rounded-2xl bg-gradient-to-br from-slate-50 to-slate-100 flex items-center justify-center group-hover:scale-110 transition-transform duration-300`}>
                    <feature.icon className={`h-8 w-8 ${feature.color}`} />
                  </div>
                </div>
                <h3 className="text-xl font-bold text-slate-800 mb-4">
                  {feature.title}
                </h3>
                <p className="text-slate-600 leading-relaxed">
                  {feature.description}
                </p>
              </CardContent>
            </Card>
          </motion.div>
        ))}
      </div>
    </div>
  );
}